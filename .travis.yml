# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
warnings_are_errors: false
r_github_packages:
  - reptalex/WrightFisher
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libgmp3-dev
